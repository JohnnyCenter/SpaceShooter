using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scrEngines : MonoBehaviour
{
    private void Start() //This will run when this object is instantiated (or sat to active) for the first time
    {
        print(gameObject + " is set to active");
    }
}
